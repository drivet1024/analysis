services:
  conveyor-dashboard:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: conveyor-dashboard

    restart: unless-stopped

    ports:
      - "8080:8080"

    env_file:
      - ${APP_ENV_FILE:-./ConveyorDashboard/.env.local}

    environment:
      ASPNETCORE_URLS: http://+:8080
      ASPNETCORE_ENVIRONMENT: Production
      TZ: America/Toronto
      DEPLOYED_AT: ${DEPLOYED_AT:-}
      EDI_FORECAST_PATH: /app/App_Data/edi-forecasts
      EDI_ML_MODEL_PATH: /app/App_Data/ml-models/edi
      CONVEYOR_EFFICIENCY_PATH: /app/App_Data/conveyor-efficiency.json

    volumes:
      - type: bind
        source: ${APP_DATA_PATH:-/opt/conveyordashboard/data}
        target: /app/App_Data
