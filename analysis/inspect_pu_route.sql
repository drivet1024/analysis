SELECT c.PU_ROUTE_ID AS route_id, COUNT(DISTINCT c.CUSTOMER_ID) AS clients
FROM customer c
WHERE c.ACTIVE = 1
  AND c.PU_ROUTE_ID IN (50103, 50104, 50125, 50129, 50130, 50144)
  AND LOWER(c.NAME) NOT LIKE '%nationex%'
GROUP BY c.PU_ROUTE_ID
ORDER BY c.PU_ROUTE_ID;
